package com.csc415.photoeditor.util

enum class EditorButtonEnum(val text : String) {
    BALANCE(text = "Filter: Balance"),
    EXPOSE(text = "Filter: Expose"),
    INVERT_COLORS(text = "Filter: Invert Colors"),
    SAVE(text = "Save"),
    SHARE(text = "Share"),
    EXIT(text = "Exit"),
    UNDO(text = "Undo"),
    ROTATE(text = "Rotate")
}